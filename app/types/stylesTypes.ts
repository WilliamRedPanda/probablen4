export type ThemeType = {
  backgroundColor: string;
  secondaryBackground: string; //card ect
  tertiaryBackground: string;
  navBarBackground: string;
  textColor: string;
  sizes: SizeType;
};

export type SizeType = {
  xs: string;
  s: string;
  m: string;
  l: string;
};
